import 'package:smartcook_cor/models/index.dart';

class RecipeRepository {
  static List<Recipe> recipes = [
    // Your initial list of recipes (can be empty as Firestore will be used to fetch recipes).
  ];
}
